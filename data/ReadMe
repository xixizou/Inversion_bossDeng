文件说明：
两组用于训练的合成数据，各40000对样本，一组实际数据
数据1. 输入100——输出300
    输入：Input_long.txt （信号，维度为40000x100，每一行为一个时间序列1x100，共40000个）
             Height_long.txt  （接收点高度，维度为40000x1，每一行为采集到数据的仪器高度1x1，共40000个）
    输出：Output_long.txt  （Label，600m深度范围电阻率值，维度为40000x300，每一行为接收点正下方的电阻率1x300，共40000个）

数据2. 输入17——输出19
    输入：Input_short.txt （信号，维度为40000x17，每一行为一个时间序列1x17，共40000个）
             Height_short.txt  （接收点高度，维度为40000x1，每一行为采集到数据的仪器高度1x1，共40000个）
    输出：Output_short.txt  （Label，380m深度范围电阻率值，维度为40000x19，每一行为接收点正下方的电阻率1x19，共40000个）

数据3.输入17——输出19（实际数据）  
    输入：Signal\signal.txt （信号，长度为17，行数不固定）
              Height\height.txt （接收点高度，长度为1）
    输出：ModelLabel\model.txt（Label，长度为19）
    命名序列：10040:10:10920, 10940:10:11050, 19010:10:19030，共104组
